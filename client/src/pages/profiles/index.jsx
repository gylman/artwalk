import { Layout } from "../../components/Layout";

export function Profile() {
  return (
    <Layout
      style={{
        display: "flex",
        flexDirection: "column",
      }}
    >
      Profile!
    </Layout>
  );
}
