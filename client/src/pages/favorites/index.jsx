import { Layout } from "../../components/Layout";

export function Favorites() {
  return (
    <Layout
      style={{
        display: "flex",
        flexDirection: "column",
      }}
    >
    </Layout>
  );
}
