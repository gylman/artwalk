// prettier.config.js
import {Options} from 'prettier'

/**
 * @type {Options}
 */
const config = {
    arrowParens: 'always',
    printWidth: 80,
    singleQuote: false,
    jsxSingleQuote: false,
    semi: true,
    trailingComma: 'all',
    tabWidth: 2,
    plugins: [],
  };
  
  module.exports = config;